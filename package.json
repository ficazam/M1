{
  "dependencies": {
    "bun": "^1.2.22"
  },
  "name": "m1",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "@changesets/cli": "^2.29.7",
    "@types/bun": "latest",
    "playwright": "^1.55.0",
    "tsup": "^8.5.0",
    "turbo": "^2.5.6",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.9.2"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "changeset": "changeset",
    "release": "changeset version && turbo run build && changeset publish",
    "format": "biome format .",
    "format_check": "biome check .",
    "clean": "turbo run clean && rm -rf node_modules .turbo"
  }
}