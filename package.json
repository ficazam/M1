{
  "name": "m1",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": ["apps/*", "api/*", "lib/*", "packages/*"],
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.9.2",
    "turbo": "^2.5.6",
    "vitest": "^3.2.4",
    "@types/node": "^24.5.0"
  },
  "dependencies": {
    "zod": "^4.1.11"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "clean": "turbo run clean && rm -rf node_modules .turbo",
    "test": "turbo run test",
    "check:zod": "node tools/check-zod.cjs"
  }
}
